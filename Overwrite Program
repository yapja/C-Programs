#include <stdio.h>
#include <stdlib.h>

main()
{
      char fileName[51];
      FILE *pFile;

      printf("Enter filename: ");
      gets(fileName);

      pFile = fopen(fileName,"wt");
      fprintf(pFile,"00000000");
      fclose(pFile);
}
